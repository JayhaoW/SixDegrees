#include <fstream>

#include "Twitter.h"

int main(int argc, char *argv[]){
	//char * file_name = argv[1];
/*
	std::ifstream infile(file_name);

	Twitter* tweet = new Twitter();

	tweet->loadGraph(infile);
	
	std::stack<User *> finished1;

	tweet->DFS(finished1);

	std::stack<User * > finished2;
	
	tweet->DFS(finished2);
	*/

}
